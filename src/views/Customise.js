import React from 'react';
import { ScrollView, View, Text} from 'react-native';

const Customise = ({ }) => {
  return (
    <ScrollView>
      <View>
        <Text>Customise</Text>
      </View>
    </ScrollView>
  )
}

export default Customise;
